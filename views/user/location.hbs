<style>
    .fixed-size-image {
        width: 300px;
        /* Set a fixed width */
        height: 200px;
        /* Set a fixed height */
        object-fit: fill;
        /* Adjusts the image to cover the container without distortion */
    }
    .activityDisplay{
        background: linear-gradient(rgb(255, 255, 255), rgba(19, 175, 226, 0.7));;
    }
</style>
<section>
    <div class="banner border-bottom pb-4">
        <img src="/location-images/Locations/{{location._id}}/{{location._id}}_Image.jpg" class="banner-img"
            style="margin-top: -2%">

        {{!-- Weather API --}}
        <div class="weather-card">

            {{!-- <div class="error">
                <p>Invalid city name</p>
            </div> --}}

            <div class="weather">

                <img src="/images/weatherAPI/{{weatherData.icon}}" class="weather-icon" style="height: 200;
        width: 300;">
                <h1 class="temp">{{weatherData.temp}}</h1>
                <h2 class="city">{{weatherData.city}}</h2>
                <div class="row">
                    <div class="weather-col">
                        <img src="/images/weatherAPI/humidity.png">
                        <div>
                            <p class="humidity">{{weatherData.humidity}}</p>
                            <p>Humidity</p>
                        </div>
                    </div>
                    <div class="weather-col">
                        <img src="/images/weatherAPI/wind.png">
                        <div>
                            <p class="wind">{{weatherData.wind}}</p>
                            <p>Wind</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    {{!-- --}}


    <div class="loc-description mx-5 px-3 pt-2">
        <p>
            {{location.Description}}
        </p>
    </div>
    <h3 class="text-center mb-4">Summer Activities</h3>
    <div class="container mt-5">
        <div class="row" style="margin-bottom : 50px;">
            <div class="col-md-4" style="">
                <img class="fixed-size-image rounded"
                    src="/location-images/Locations/{{location._id}}/{{location._id}}_ClearImage1.jpg"
                    class="img-fluid rounded" alt="Attraction 1">
            </div>
            <div class="col-md-4">
                <img class="fixed-size-image rounded"
                    src="/location-images/Locations/{{location._id}}/{{location._id}}_ClearImage2.jpg"
                    class="img-fluid rounded" alt="Attraction 2">
            </div>
            <div class="col-md-4">
                <img class="fixed-size-image rounded"
                    src="/location-images/Locations/{{location._id}}/{{location._id}}_ClearImage3.jpg"
                    class="img-fluid rounded" alt="Attraction 3">
            </div>
        </div>
        <div class="row">
            {{#each clearActivities}}
            <div class="card activityDisplay" style="width: 18rem; padding: 10px; margin-left: 20px; margin-right : 70px;">
                {{this}}</div>
            {{/each}}
        </ul></div>
            


            <div class="container mt-5">
                <h3 class="text-center mb-4">Monsoon Activities</h3>
                <div class="row" style="margin-bottom : 50px;">
                    <div class="col-md-4" style="">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_RainyImage1.jpg"
                            class="img-fluid rounded" alt="Attraction 1">
                    </div>
                    <div class="col-md-4">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_RainyImage2.jpg"
                            class="img-fluid rounded" alt="Attraction 2">
                    </div>
                    <div class="col-md-4">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_RainyImage3.jpg"
                            class="img-fluid rounded" alt="Attraction 3">
                    </div>
                </div>
        <div class="row">
            {{#each rainyActivities}}
            <div class="card activityDisplay" style="width: 18rem; padding: 10px; margin-left: 20px; margin-right : 60px;">
                {{this}}</div>
            {{/each}}
            </div>
            <div class="container mt-5">
<h3 class="text-center mb-4">Winter Activities</h3>
                <div class="row" style="margin-bottom : 50px;">
                    <div class="col-md-4" style="">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_WindyImage1.jpg"
                            alt="Attraction 1">
                    </div>
                    <div class="col-md-4">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_WindyImage2.jpg"
                            alt="Attraction 2">
                    </div>
                    <div class="col-md-4">
                        <img class="fixed-size-image rounded"
                            src="/location-images/Locations/{{location._id}}/{{location._id}}_WindyImage3.jpg"
                             alt="Attraction 3">
                    </div>
                </div>
                    <div class="row">
            {{#each windyActivities}}
            <div class="card activityDisplay" style="width: 18rem; padding: 10px; margin-left: 20px; margin-right : 50px;">
                {{this}}</div>
            {{/each}}
            </div>
<div style="padding-top: 100px;"><h2>Suggestions</h2>
    {{#if suggestion}}
    {{#each suggestion}}
    <div class="container my-4">
        <h2 class="text-center mb-4"></h2>
        <div class="row">
            <!-- Card 1 -->
            <div class="col-md-4 mb-4">
                <div class="card h-100 shadow-sm">
                    <div class="card-body">
                        <h5 class="card-title">{{this.user}}</h5>
                        <p class="card-text">{{this.suggestion}}</p>
                    </div>
                </div>
            </div>
            {{/each}}
            {{/if}}

</div>
        </div>
    </div>
    


        <!-- ... your existing HTML code ... -->

        <script>
            // Assume you have a variable 'weatherCondition' with values like 'Clear', 'Rainy', 'Windy'
            //var weatherCondition = {{ weatherData.weather }};  // Replace this with the actual weather condition
            var weatherCondition = 'windy';  // Replace this with the actual weather condition

            // Function to add weather-specific classes to activities
            function addWeatherClasses(weather) {
                var activityContainers = document.querySelectorAll('.loc-activities .card');

                activityContainers.forEach(function (activityContainer) {
                    // Extract the weather type from the activity container's class
                    var activityWeatherType = activityContainer.classList[0].replace('weather-', '');

                    // Check if the weather type matches the current weather condition
                    if (activityWeatherType.toLowerCase() === weather.toLowerCase()) {
                        // Add a class to highlight or modify the styling for matching activities
                        activityContainer.classList.add('highlight-activity');
                    }
                });
            }

            // Example: You might get the weather condition from your API response
            // Replace this with the actual code to get weather condition from your API
            // For demonstration, using a fixed value here
            addWeatherClasses(weatherCondition);
        </script>

        <style>
            /* ... your existing CSS styles ... */

            .highlight-activity::before {
                background: rgba(0, 0, 0, 0.5);
                /* Adjust the alpha value for transparency */
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                /*z-index: 1;
             Adjust the z-index if needed */
            }

            .highlight-activity .card::before {
                display: flex;
                justify-content: center;
                align-items: center;
                color: white;
                /* Text color on the overlay */
                font-size: 18px;
                /* Adjust font size */
            }
        </style>